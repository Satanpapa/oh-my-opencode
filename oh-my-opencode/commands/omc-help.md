---
description: Show oh-my-opencode help — execution modes, available agents, magic keywords
---

Use the `omc-modes` tool to display the full list of execution modes and magic keywords.

Then show the available specialized subagents:

```
@architect    — System design & architecture decisions
@researcher   — Deep research & codebase analysis
@debugger     — Bug hunting & root cause analysis
@tester       — Test writing & QA strategy
@security     — Security audits & vulnerability detection
@performance  — Profiling & optimization
@docs-writer  — README, API docs, code comments
@refactorer   — Code cleanup & pattern application
@reviewer     — Code review & best practices
@devops       — Docker, CI/CD, deployment, infra
@frontend     — UI/UX, React, CSS, accessibility
@api-designer — REST/GraphQL/RPC API design
@database     — Schema design, queries, migrations
```

Also show the oh-my-opencode version using the `omc-version` tool.
